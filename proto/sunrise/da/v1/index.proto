syntax = "proto3";
package sunrise.da.v1;

option go_package = "github.com/sunriselayer/sunrise/x/da/types";

// ShardIndex
message ShardIndex {
  uint32 row_index = 1;
  // must be 64n (64 = ElementsLenPerShard = SrsLen * ElementSize / ShardSize)
  uint32 col_index = 2;
}
