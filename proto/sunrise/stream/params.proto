syntax = "proto3";
package sunrise.stream;

import "amino/amino.proto";
import "gogoproto/gogo.proto";

option go_package = "sunrise/x/stream/types";

// Params defines the parameters for the module.
message Params {
  option (amino.name) = "sunrise/x/stream/Params";
  option (gogoproto.equal) = true;

  
}