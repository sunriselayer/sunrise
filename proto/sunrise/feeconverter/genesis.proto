syntax = "proto3";
package sunrise.feeconverter;

import "amino/amino.proto";
import "gogoproto/gogo.proto";
import "sunrise/feeconverter/params.proto";

option go_package = "github.com/sunriselayer/sunrise/x/feeconverter/types";

// GenesisState defines the feeconverter module's genesis state.
message GenesisState {
  // params defines all the parameters of the module.
  Params params = 1 [
    (gogoproto.nullable) = false,
    (amino.dont_omitempty) = true
  ];
}
