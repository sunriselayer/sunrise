syntax = "proto3";
package sunrise.grant;

import "amino/amino.proto";
import "gogoproto/gogo.proto";

option go_package = "sunrise/x/grant/types";

// Params defines the parameters for the module.
message Params {
  option (amino.name) = "sunrise/x/grant/Params";
  option (gogoproto.equal) = true;

  
}