syntax = "proto3";
package sunrise.rfq.v1;

import "amino/amino.proto";
import "gogoproto/gogo.proto";
import "sunrise/rfq/v1/params.proto";

option go_package = "github.com/sunriselayer/sunrise/x/rfq/types";

// GenesisState defines the rfq module's genesis state.
message GenesisState {
  // params defines all the parameters of the module.
  Params params = 1 [
    (gogoproto.nullable) = false,
    (amino.dont_omitempty) = true
  ];
}
